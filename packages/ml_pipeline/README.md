# HydroOnc Pipeline

Phase 7 connects all HydroOnc packages into a unified multi-scale research
pipeline and exposes the `hydroonc` CLI.

Install:

```bash
pip install -e "packages/ml_pipeline[dev]"
```

Run use case 1:

```bash
hydroonc pipeline use-case-1 --patient-data patient.json
```
